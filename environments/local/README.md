# Local development environment

Purpose: repository work, documentation, standard-library tests, manifest
verification, and lightweight analysis. Large datasets and routine checkpoints
must not be stored inside the repository.

The first foundation release requires Python 3.10 or newer and has no runtime
third-party dependency. ML and robotics dependencies will be added in isolated,
pinned groups after their architecture decisions are approved.
